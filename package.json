{
  "name": "tason-monorepo",
  "private": true,
  "description": "TASON monorepo — core package and type extensions",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test",
    "generate": "yarn workspace tason generate",
    "build:tason": "yarn workspace tason build",
    "build:mongodb": "yarn workspace tason-mongodb build",
    "test:tason": "yarn workspace tason test",
    "test:mongodb": "yarn workspace tason-mongodb test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/SwingCosmic/tason.git"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
