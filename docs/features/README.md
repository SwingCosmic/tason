# Features

按 feature 分目录存放设计与实施文档，避免互相覆盖。

| 目录 | 主题 | 状态 |
| --- | --- | --- |
| [runtime-type/](./runtime-type/) | 数值 Handling · 运行时契约 (schema) · 鸭子类型 | 阶段 1 done · 2.1 next |

新增 feature 时：在本目录下新建子目录（含 `README.md` + 设计/进度），不要把文件直接堆在 `features/` 根下。
