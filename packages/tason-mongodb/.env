# tason-mongodb 集成测试连接信息（占位值，勿填真实凭据）
# 真实连接信息写入同目录 .env.local（已被 gitignore），或通过进程环境变量注入；
# 优先级：进程环境变量 > .env.local > .env
MONGODB_URI=mongodb://placeholder.example.com:27017
MONGODB_DB_NAME=tason_integration_test
